#include<stdio.h>
int main()
{
	int 
	first=0;
	last=size-1; 
	middle=first+last/2;
	while(first<=last
	if(a[middle]<=se)
	first=middle+1;
	else if(a[middle]==se)
	{
		printf("se identified %d",middle);
		break;
	}
	else
	last=middle-1;
	middle=first+last/2;
}
if(first>last){
	printf("no such elements");
}
return 0;
	}
#include<stdio.h>
void main()
{
	void insertbegin(int);
	void insertend(int);
	void insertbetween(int);
	void display();
	void deletebegin(int);
	void deleteend();
	void deletebetween(int);
	struct node
	{
		int data;
		struct node*next;
	}
	*head=NULL;
	void main()
	void insertbegin(intvalue)
	{
		struct node*newnode;
		new node=(structnode*))
		if(head==NULL)
		{
			head=newnode;}
		}
		else
		head=newnode;
	}
	printf("\n one node insert");
	}
	
}